# SPEC_03 엑셀 다운로드 기능 배포 가이드

## 📋 개요

입출금 내역 및 회사비용을 엑셀(CSV) 파일로 다운로드하는 기능을 완성하기 위한 배포 가이드입니다.

## ✅ 완료된 작업

1. **백엔드 서비스 구현** (`ExcelExportService.js`)
   - 입출금 내역 CSV 변환
   - 회사비용 CSV 변환
   - 자동 파일명 생성
   - UTF-8 BOM 지원 (한글 깨짐 방지)

2. **프론트엔드 UI 추가** (`Page_PaymentManagement.html`)
   - 입출금 관리 탭: "📥 엑셀 다운로드" 버튼
   - 회사비용 관리 탭: "📥 엑셀 다운로드" 버튼

3. **다운로드 핸들러 구현** (`CommonScripts.html`)
   - `downloadPaymentExcel()` 함수
   - `downloadExpenseExcel()` 함수

4. **Web App 설정** (`appsscript.json`)
   - `access: "ANYONE"` 설정 완료

## 🚀 배포 절차

### 1단계: Google Apps Script 프로젝트에 코드 업로드

다음 파일들을 Google Apps Script 프로젝트에 업로드하거나 업데이트:

```
✅ ExcelExportService.js (신규)
✅ Page_PaymentManagement.html (수정)
✅ CommonScripts.html (수정)
✅ appsscript.json (수정)
```

### 2단계: Web App 배포

1. **Google Apps Script 에디터 열기**
   - 스프레드시트 → 확장 프로그램 → Apps Script

2. **새 배포 생성**
   - 상단 메뉴에서 **"배포"** → **"새 배포"** 클릭

3. **배포 설정**
   - 유형 선택: **"웹 앱"** ⚙️ 클릭
   - 설명: `SPEC_03 엑셀 다운로드 서비스`
   - 다음 사용자로 실행: **"나"**
   - 액세스 권한: **"모든 사용자"**

4. **배포 버튼 클릭**
   - 권한 승인 필요시 승인 진행
   - 배포 완료 후 **배포 ID**와 **웹 앱 URL** 복사

   예시:
   ```
   웹 앱 URL: https://script.google.com/macros/s/AKfycbx.../exec
   ```

### 3단계: Web App URL 설정

1. **CommonScripts.html 파일 수정**

2. **두 곳의 URL 교체** (약 6129번째 줄, 6164번째 줄):

   ```javascript
   // 수정 전
   var WEB_APP_URL = 'YOUR_WEB_APP_URL_HERE';  // TODO: 배포 후 교체

   // 수정 후 (실제 배포된 URL로 교체)
   var WEB_APP_URL = 'https://script.google.com/macros/s/AKfycbx.../exec';
   ```

3. **변경사항 저장 및 재배포**

### 4단계: 테스트

1. **입출금 내역 다운로드 테스트**
   - 결제관리 페이지 → 입출금 관리 탭
   - 필터 조건 설정 (선택사항)
   - "📥 엑셀 다운로드" 버튼 클릭
   - CSV 파일 다운로드 확인
   - 엑셀에서 파일 열어 한글 정상 표시 확인

2. **회사비용 다운로드 테스트**
   - 결제관리 페이지 → 회사비용 관리 탭
   - 필터 조건 설정 (선택사항)
   - "📥 엑셀 다운로드" 버튼 클릭
   - CSV 파일 다운로드 확인

## 📁 파일명 생성 규칙

다운로드되는 파일명은 자동으로 생성됩니다:

### 입출금 내역
```
입출금내역_20260101_20260131.csv         (기간 필터)
입출금내역_20260101_20260131_매입.csv    (유형 필터 포함)
입출금내역_전체.csv                      (필터 없음)
입출금내역_20260101_매입_ABC거래처.csv   (복합 필터)
```

### 회사비용
```
회사비용_20260101_20260131.csv           (기간 필터)
회사비용_20260101_20260131_인건비.csv    (분류 필터 포함)
회사비용_전체.csv                        (필터 없음)
```

## 📊 CSV 파일 구조

### 입출금 내역 CSV

```csv
기간: 2026-01-01 ~ 2026-01-31 | 유형: 매입

결제ID,결제일,유형,거래처,금액,결제수단,문서번호,발주번호,비고,입력일시
PAY001,2026-01-15,출금,ABC거래처,1000000,계좌이체,DOC001,ORD001,,2026-01-15 10:30:00
...

총 10건,합계: 10,000,000원
```

### 회사비용 CSV

```csv
기간: 2026-01-01 ~ 2026-01-31

비용ID,비용일,비용항목,금액,결제수단,비고,입력일시
EXP001,2026-01-15,인건비,2000000,계좌이체,급여,2026-01-15 10:30:00
...

총 5건,합계: 5,000,000원
```

## 🔧 기술 세부사항

### UTF-8 BOM 처리
```javascript
var BOM = '\uFEFF';
var csvWithBOM = BOM + result.csv;
```
- Excel에서 한글 자동 인식
- Windows/Mac 모두 지원

### 제외되는 컬럼
**입출금 내역:**
- 삭제여부
- 삭제일시
- 삭제자
- 원결제ID
- 환불여부

**회사비용:**
- 삭제여부

### 필터 적용
- 삭제된 데이터 자동 제외
- 날짜 범위 필터 (startDate, endDate)
- 유형/분류 필터
- 거래처명 부분 일치 검색

## ⚠️ 주의사항

1. **권한 설정**
   - appsscript.json의 `access: "ANYONE"` 필요
   - 배포 시 "모든 사용자" 권한 부여 필요

2. **보안**
   - 현재 설정은 URL을 아는 사람은 누구나 다운로드 가능
   - 추가 보안이 필요한 경우 토큰 인증 추가 권장

3. **대용량 데이터**
   - Google Apps Script 실행 시간 제한: 6분
   - 대량 데이터는 날짜 필터로 분할 다운로드 권장

4. **브라우저 호환성**
   - `window.open()` 사용으로 팝업 차단 해제 필요
   - Chrome, Edge, Safari 모두 지원

## 🐛 문제 해결

### 파일이 다운로드되지 않을 때
1. 팝업 차단 해제 확인
2. Web App URL이 정확히 설정되었는지 확인
3. 배포 권한이 "모든 사용자"인지 확인

### 한글이 깨질 때
1. UTF-8 BOM이 정상 추가되었는지 확인
2. Excel에서 파일 열기 시 인코딩 수동 선택: UTF-8

### 오류 메시지가 표시될 때
1. Apps Script 로그 확인 (실행 → 실행 로그)
2. 스프레드시트 ID (SS_ID) 정상 설정 확인
3. 시트명 확인: '결제내역', '회사비용'

## ✅ 체크리스트

배포 전 확인사항:

- [ ] ExcelExportService.js 업로드 완료
- [ ] Page_PaymentManagement.html 수정 완료
- [ ] CommonScripts.html 수정 완료
- [ ] appsscript.json 수정 완료 (access: "ANYONE")
- [ ] Web App 배포 완료
- [ ] Web App URL 복사 완료
- [ ] CommonScripts.html에 URL 설정 완료
- [ ] 입출금 다운로드 테스트 성공
- [ ] 회사비용 다운로드 테스트 성공
- [ ] 한글 표시 확인 완료

---

**배포 완료 후 다음 단계:**
- SPEC_04: 결제 예정 알림 (이메일 알림 시스템)
- SPEC_05: 미수금 관리 (전용 관리 페이지)
