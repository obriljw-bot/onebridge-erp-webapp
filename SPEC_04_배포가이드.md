# SPEC_04 배포 가이드

## 1단계: 데이터베이스 마이그레이션

### 1.1 청구DB 컬럼 추가

Apps Script 편집기에서 다음 함수 실행:

```javascript
migrateInvoiceSheetForAlerts()
```

**실행 방법:**
1. Apps Script 편집기 열기
2. 함수 선택: `migrateInvoiceSheetForAlerts`
3. 실행 버튼 클릭
4. 권한 승인 (처음 실행 시)

**결과 확인:**
- 청구DB 시트에 `결제예정일`, `알림담당자` 컬럼 추가됨
- 로그: "✅ 청구DB 컬럼 추가 완료: 19개 → 21개"

### 1.2 알림설정 시트 생성

```javascript
setupAlertSettings()
```

**결과 확인:**
- 새 시트 `알림설정` 생성됨
- 5개 알림 설정 자동 입력:
  - D-7 매입 결제
  - D-3 매입 결제
  - D-day 매입 결제
  - D-3 매출 입금
  - D-day 매출 입금

## 2단계: 테스트 데이터 입력

### 2.1 청구DB에 결제예정일 입력

1. 청구DB 시트 열기
2. 기존 청구서 중 하나 선택
3. `결제예정일` 컬럼에 **오늘 날짜** 입력
4. `알림담당자` 컬럼에 본인 이메일 입력

예시:
| 청구ID | 청구유형 | 거래처명 | 청구상태 | 결제예정일 | 알림담당자 |
|--------|---------|---------|---------|-----------|-----------|
| INV-001 | 매입 | 삼성전자 | ISSUED | 2026-01-03 | your@email.com |

## 3단계: 시간 기반 트리거 설정

### 3.1 트리거 함수 실행

```javascript
setupDailyAlertTrigger()
```

**결과 확인:**
- 로그: "✅ 매일 오전 9시 알림 트리거 설정 완료"

### 3.2 트리거 확인

1. Apps Script 편집기 → 트리거 (왼쪽 메뉴 시계 아이콘)
2. 트리거 목록에서 확인:
   - 함수: `dailyPaymentAlertCheck`
   - 시간 기반: 매일 오전 9-10시

## 4단계: 수동 테스트

### 4.1 알림 이메일 발송 테스트

```javascript
dailyPaymentAlertCheck()
```

**결과 확인:**
- 로그에서 "알림 발송" 또는 "알림 없음" 확인
- 이메일 수신함 확인

**예상 이메일:**
```
제목: [결제 알림] D-day (오늘) 결제 예정 청구서 1건

안녕하세요,

오늘 결제 예정인 청구서가 1건 있습니다:

1. 청구ID: INV-001
   거래처: 삼성전자
   금액: 5,000,000원
   예정일: 2026-01-03

총 결제 예정 금액: 5,000,000원

[청구서 보기]
https://script.google.com/...

감사합니다.
원브릿지 ERP
```

### 4.2 웹 위젯 테스트

1. ERP 웹앱 접속
2. 결제관리 페이지 열기
3. 상단에 알림 위젯 표시 확인:
   - "오늘 결제 예정 (매입): X건"
   - "오늘 입금 예정 (매출): X건"

## 5단계: 알림설정 시트 수정

### 5.1 수신자 이메일 변경

1. `알림설정` 시트 열기
2. `수신자이메일` 컬럼 수정:
   - 매입 알림: `finance@company.com` → 실제 재무팀 이메일
   - 매출 알림: `sales@company.com` → 실제 영업팀 이메일

### 5.2 알림 활성화/비활성화

- `활성여부` 컬럼을 `TRUE`/`FALSE`로 변경
- 비활성화된 알림은 발송되지 않음

## 6단계: 운영 모니터링

### 6.1 로그 확인

1. Apps Script 편집기 → 실행 로그
2. 매일 오전 9시 이후 로그 확인:
   ```
   ========================================
   결제 예정 알림 체크 시작: 2026-01-03 09:00:00
   ========================================
   [알림 발송] 매입 D-day: 3건
   [알림 없음] 매입 D-3: 0건
   ...
   ========================================
   알림 체크 완료
   ========================================
   ```

### 6.2 트러블슈팅

**이메일이 발송되지 않는 경우:**
- 청구DB에 `결제예정일` 입력 확인
- `청구상태`가 `ISSUED` 또는 `PAID_PARTIAL`인지 확인
- 알림설정 시트에서 `활성여부`가 `TRUE`인지 확인
- MailApp 일일 한도 확인 (100통/일)

**위젯이 표시되지 않는 경우:**
- 브라우저 콘솔 (F12) 에러 확인
- 오늘 결제 예정 청구서가 있는지 확인

## 7단계: 다음 구현 (SPEC_05)

SPEC_04 완료 후 다음 단계:
- [ ] SPEC_05: 미수금 관리 전용 페이지
- [ ] D-day 컬럼 추가 (청구서 목록)
- [ ] 위젯 상세보기 기능

---

**문서 끝**
