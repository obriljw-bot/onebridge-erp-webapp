# 결제관리 시스템 테스트 체크리스트 (Phase 6)

## 📋 테스트 환경 준비

### 1. 초기 설정
- [ ] Google Apps Script 프로젝트에 모든 파일 업로드 확인
- [ ] SetupPaymentSheets.js 실행하여 시트 생성
- [ ] 스프레드시트 ID가 올바르게 설정되어 있는지 확인
  - PaymentService.js의 `PAYMENT_SS_ID` 확인

---

## 🧪 Phase 1: 데이터 구조 테스트

### 1.1 시트 생성 테스트
```javascript
// Apps Script에서 실행
setupPaymentSheets();
```

**확인 사항:**
- [ ] [결제내역] 시트가 생성되었는가?
  - [ ] 14개 컬럼이 모두 있는가? (결제ID, 결제일, 결제유형, 거래처명, 금액, 결제수단, 문서번호, 발주번호, 비고, 삭제여부, 삭제일시, 삭제자, 입력일시, 입력자)
  - [ ] 헤더 행이 올바른가?

- [ ] [회사비용] 시트가 생성되었는가?
  - [ ] 11개 컬럼이 모두 있는가? (비용ID, 비용일, 비용항목, 금액, 결제수단, 비고, 삭제여부, 삭제일시, 삭제자, 입력일시, 입력자)

- [ ] [청구DB] 시트에 컬럼이 추가되었는가?
  - [ ] '대체청구서' 컬럼이 있는가?
  - [ ] '원본청구서' 컬럼이 있는가?

### 1.2 데이터 유효성 검사 테스트
- [ ] [결제내역] C열(결제유형): 드롭다운 '입금/출금' 동작하는가?
- [ ] [결제내역] F열(결제수단): 드롭다운 '현금/카드/계좌이체/기타' 동작하는가?
- [ ] [결제내역] J열(삭제여부): 체크박스로 표시되는가?
- [ ] [회사비용] C열(비용항목): 드롭다운 '인건비/임차료/통신비...' 동작하는가?
- [ ] [회사비용] E열(결제수단): 드롭다운이 동작하는가?
- [ ] [회사비용] G열(삭제여부): 체크박스로 표시되는가?

---

## 🔧 Phase 2: 백엔드 서비스 테스트

### 2.1 입출금 관리 함수 테스트

#### 2.1.1 입출금 추가 (addPaymentRecord)
```javascript
var result = addPaymentRecord({
  date: '2025-01-15',
  type: '입금',
  company: '테스트거래처',
  amount: 1000000,
  method: '계좌이체',
  docNumber: 'INV-20250115-001',
  orderNumber: 'GMP-20250115-001',
  notes: '테스트 입금'
});
Logger.log(result);
```

**확인 사항:**
- [ ] `success: true`가 반환되는가?
- [ ] `paymentId`가 "PAY-YYYYMMDD-001" 형식인가?
- [ ] [결제내역] 시트에 데이터가 추가되었는가?
- [ ] 입력일시와 입력자가 자동으로 기록되었는가?

#### 2.1.2 입출금 조회 (getPaymentRecords)
```javascript
// 전체 조회
var result = getPaymentRecords({});
Logger.log(result);

// 유형 필터
var result2 = getPaymentRecords({ type: '입금' });
Logger.log(result2);

// 거래처 필터
var result3 = getPaymentRecords({ company: '테스트' });
Logger.log(result3);

// 날짜 범위 필터
var result4 = getPaymentRecords({
  startDate: '2025-01-01',
  endDate: '2025-01-31'
});
Logger.log(result4);
```

**확인 사항:**
- [ ] 전체 조회가 동작하는가?
- [ ] 유형 필터링이 정확한가?
- [ ] 거래처 부분일치 검색이 동작하는가?
- [ ] 날짜 범위 필터링이 정확한가?
- [ ] 삭제된 항목이 기본적으로 제외되는가?
- [ ] `includeDeleted: true`로 삭제된 항목도 조회되는가?

#### 2.1.3 입출금 수정 (updatePaymentRecord)
```javascript
var result = updatePaymentRecord({
  paymentId: 'PAY-20250115-001',
  amount: 1500000,
  notes: '수정된 비고'
});
Logger.log(result);
```

**확인 사항:**
- [ ] 수정이 성공하는가?
- [ ] [결제내역] 시트의 데이터가 업데이트되었는가?

#### 2.1.4 입출금 삭제 (deletePaymentRecord)
```javascript
var result = deletePaymentRecord({
  paymentId: 'PAY-20250115-001'
});
Logger.log(result);
```

**확인 사항:**
- [ ] `success: true`가 반환되는가?
- [ ] 시트에서 행이 삭제되지 않고 남아있는가?
- [ ] 삭제여부 컬럼이 `true`로 변경되었는가?
- [ ] 삭제일시가 기록되었는가?
- [ ] 삭제자가 기록되었는가?

#### 2.1.5 입출금 통계 (getPaymentSummary)
```javascript
// 여러 입출금 데이터 추가 후
var result = getPaymentSummary({
  startDate: '2025-01-01',
  endDate: '2025-01-31'
});
Logger.log(result);
```

**확인 사항:**
- [ ] `totalIncome` (총 입금)이 정확한가?
- [ ] `totalExpense` (총 출금)이 정확한가?
- [ ] `netProfit` (순이익) = totalIncome - totalExpense 계산이 맞는가?
- [ ] 삭제된 항목은 통계에서 제외되는가?

#### 2.1.6 문서번호 자동완성 (searchDocumentNumbers)
```javascript
// [청구DB] 시트에 청구서 데이터가 있어야 함
var result = searchDocumentNumbers({
  query: 'INV-202'
});
Logger.log(result);
```

**확인 사항:**
- [ ] 3글자 미만은 빈 배열을 반환하는가?
- [ ] 부분일치 검색이 동작하는가?
- [ ] 대소문자 구분 없이 검색되는가?
- [ ] CANCELLED 상태 청구서는 제외되는가?
- [ ] 최대 10개만 반환되는가?
- [ ] company, invoiceDate, amount 정보가 포함되는가?

### 2.2 회사비용 관리 함수 테스트

#### 2.2.1 비용 추가 (addExpenseRecord)
```javascript
var result = addExpenseRecord({
  date: '2025-01-15',
  category: '임차료',
  amount: 2000000,
  method: '계좌이체',
  notes: '사무실 월세'
});
Logger.log(result);
```

**확인 사항:**
- [ ] `success: true`가 반환되는가?
- [ ] `expenseId`가 "EXP-YYYYMMDD-001" 형식인가?
- [ ] [회사비용] 시트에 데이터가 추가되었는가?

#### 2.2.2 비용 조회 (getExpenseRecords)
```javascript
var result = getExpenseRecords({
  category: '임차료',
  startDate: '2025-01-01',
  endDate: '2025-01-31'
});
Logger.log(result);
```

**확인 사항:**
- [ ] 비용항목 필터링이 동작하는가?
- [ ] 날짜 범위 필터링이 동작하는가?

#### 2.2.3 비용 통계 (getExpenseSummary)
```javascript
// 여러 비용항목 데이터 추가 후
var result = getExpenseSummary({});
Logger.log(result);
```

**확인 사항:**
- [ ] `totalExpense`가 정확한가?
- [ ] `byCategory` 객체가 항목별 합계를 정확히 계산하는가?

### 2.3 청구서 이력 함수 테스트

#### 2.3.1 청구서 취소/재발급 (cancelAndReissueInvoice)
```javascript
// [청구DB] 시트에 청구서 데이터가 있어야 함
var result = cancelAndReissueInvoice({
  invoiceId: 'INV-20250115-001',
  notes: '금액 오류로 인한 재발급'
});
Logger.log(result);
```

**확인 사항:**
- [ ] `success: true`가 반환되는가?
- [ ] `oldInvoiceId`와 `newInvoiceId`가 반환되는가?
- [ ] 원본 청구서 상태가 'CANCELLED'로 변경되었는가?
- [ ] 새 청구서가 생성되었는가?
- [ ] 원본 청구서의 '대체청구서' 컬럼에 새 ID가 기록되었는가?
- [ ] 새 청구서의 '원본청구서' 컬럼에 원본 ID가 기록되었는가?
- [ ] 새 청구서의 모든 데이터가 원본과 동일한가? (ID, 날짜, 상태 제외)

#### 2.3.2 청구서 이력 조회 (getInvoiceHistory)
```javascript
var result = getInvoiceHistory({
  invoiceId: 'INV-20250115-002'  // 재발급된 청구서 ID
});
Logger.log(result);
```

**확인 사항:**
- [ ] 이력이 시간 순서대로 반환되는가? (가장 오래된 것부터)
- [ ] 원본 → 대체 청구서 체인이 모두 조회되는가?
- [ ] 각 청구서의 상태가 정확한가?

---

## 🎨 Phase 3: 프론트엔드 UI 테스트

### 3.1 결제관리 페이지 접근
- [ ] 웹앱을 배포하고 URL에 접근할 수 있는가?
- [ ] 페이지가 정상적으로 로드되는가?
- [ ] 2개의 탭이 표시되는가? (입출금 관리, 회사비용 관리)

### 3.2 입출금 관리 탭 테스트

#### 3.2.1 입출금 추가 폼
- [ ] 오늘 날짜가 기본값으로 설정되는가?
- [ ] 모든 필수 필드가 표시되는가?
- [ ] 결제유형 드롭다운이 동작하는가? (입금/출금)
- [ ] 결제수단 드롭다운이 동작하는가?
- [ ] 문서번호 입력시 자동완성이 동작하는가?
  - [ ] 3글자 이상 입력시 드롭다운이 나타나는가?
  - [ ] 제안 목록에서 선택할 수 있는가?
- [ ] '추가' 버튼 클릭시:
  - [ ] 유효성 검사가 동작하는가? (빈 필드 체크)
  - [ ] 로딩 오버레이가 표시되는가?
  - [ ] 성공 메시지가 나타나는가?
  - [ ] 폼이 초기화되는가?
  - [ ] 목록이 자동으로 새로고침되는가?

#### 3.2.2 입출금 통계 카드
- [ ] 총 입금 금액이 표시되는가?
- [ ] 총 출금 금액이 표시되는가?
- [ ] 순이익이 계산되어 표시되는가?
- [ ] 금액이 천 단위 콤마로 포맷되는가?

#### 3.2.3 입출금 목록 조회
- [ ] 필터 없이 조회시 전체 목록이 표시되는가?
- [ ] 결제유형 필터가 동작하는가?
- [ ] 거래처 필터가 동작하는가?
- [ ] 날짜 범위 필터가 동작하는가?
- [ ] 문서번호 필터가 동작하는가?
- [ ] 삭제 포함/미포함 필터가 동작하는가?
- [ ] '초기화' 버튼이 모든 필터를 리셋하는가?

#### 3.2.4 입출금 목록 테이블
- [ ] 모든 컬럼이 표시되는가?
- [ ] 입금/출금 배지 색상이 다른가?
- [ ] 삭제된 항목은 회색으로 표시되는가?
- [ ] 금액이 천 단위 콤마로 포맷되는가?
- [ ] '수정' 버튼이 표시되는가?
- [ ] '삭제' 버튼이 표시되는가?
- [ ] 삭제된 항목은 액션 버튼이 비활성화되는가?

#### 3.2.5 입출금 삭제 기능
- [ ] '삭제' 버튼 클릭시 확인 메시지가 나타나는가?
- [ ] 삭제 후 목록이 새로고침되는가?
- [ ] 삭제된 항목이 회색으로 표시되는가?
- [ ] 통계가 업데이트되는가?

### 3.3 회사비용 관리 탭 테스트

#### 3.3.1 회사비용 추가 폼
- [ ] 비용항목 드롭다운이 9개 항목을 포함하는가?
- [ ] 추가 기능이 정상 동작하는가?

#### 3.3.2 회사비용 통계
- [ ] 총 비용이 표시되는가?
- [ ] 항목별 집계가 표시되는가?
- [ ] 항목별 금액이 줄바꿈으로 구분되어 표시되는가?

#### 3.3.3 회사비용 목록
- [ ] 필터링이 동작하는가?
- [ ] 삭제 기능이 동작하는가?

### 3.4 탭 전환 테스트
- [ ] 탭 클릭시 컨텐츠가 전환되는가?
- [ ] 탭 전환시 데이터가 리로드되는가?
- [ ] 탭 버튼 활성화 스타일이 적용되는가?

---

## 🔗 Phase 4: 거래원장 통합 테스트

### 4.1 거래원장 페이지
- [ ] 거래원장 페이지에 접근할 수 있는가?
- [ ] 거래 목록이 표시되는가?

### 4.2 거래 상세 모달
- [ ] 거래 행을 클릭하거나 상세보기 버튼을 클릭할 수 있는가?
- [ ] 상세 모달이 열리는가?
- [ ] 발주 정보가 표시되는가?
  - [ ] 발주일, 발주번호, 발주처, 브랜드
  - [ ] 발주수량, 확정수량
  - [ ] 매입액, 공급액

### 4.3 문서 정보 섹션 (결제 내역)
- [ ] '문서 정보 - 관련 결제 내역' 섹션이 표시되는가?
- [ ] 해당 발주번호와 연결된 결제 내역이 조회되는가?
- [ ] 결제 내역이 없으면 "관련 결제 내역이 없습니다" 메시지가 표시되는가?
- [ ] 결제 내역이 있으면:
  - [ ] 결제ID, 결제일, 유형, 거래처, 금액, 결제수단, 비고가 표시되는가?
  - [ ] 입금/출금 배지가 다른 색상으로 표시되는가?
  - [ ] 금액이 포맷되어 표시되는가?

### 4.4 모달 닫기
- [ ] X 버튼으로 모달을 닫을 수 있는가?
- [ ] 모달 외부 클릭시 모달이 닫히는가?

---

## 🔄 Phase 5: 청구서 취소/재발급 테스트

### 5.1 청구서 관리 페이지
- [ ] 인보이스 관리 페이지에 접근할 수 있는가?
- [ ] 청구서 목록이 표시되는가?

### 5.2 청구서 취소/재발급 모달

#### 5.2.1 모달 열기
- [ ] 청구서 목록의 액션 컬럼에 '취소/재발급' 버튼이 있는가?
- [ ] 버튼 클릭시 모달이 열리는가?
- [ ] 취소할 청구서 ID가 자동으로 입력되는가?
- [ ] 경고 메시지가 표시되는가?

#### 5.2.2 취소 실행
- [ ] 취소 사유를 입력할 수 있는가?
- [ ] '취소 및 재발급' 버튼 클릭시 확인 메시지가 나타나는가?
- [ ] 확인 후:
  - [ ] 로딩 오버레이가 표시되는가?
  - [ ] 성공 메시지가 나타나는가?
  - [ ] 원본 및 새 청구서 ID가 표시되는가?
  - [ ] 모달이 닫히는가?
  - [ ] 청구서 목록이 새로고침되는가?
  - [ ] 원본 청구서가 CANCELLED 상태로 표시되는가?
  - [ ] 새 청구서가 목록에 나타나는가?

### 5.3 청구서 이력 조회

#### 5.3.1 이력 모달 열기
- [ ] 청구서 목록의 액션 컬럼에 '이력' 버튼이 있는가?
- [ ] 버튼 클릭시 이력 모달이 열리는가?

#### 5.3.2 이력 표시
- [ ] 이력이 시간순으로 표시되는가?
- [ ] 각 청구서 카드에:
  - [ ] 청구서 ID가 표시되는가?
  - [ ] 활성/취소됨 상태가 표시되는가?
  - [ ] 거래처, 청구일, 금액, 상태 정보가 표시되는가?
  - [ ] 비고가 있으면 표시되는가?
  - [ ] 대체청구서/원본청구서 링크가 표시되는가?
- [ ] 취소된 청구서는 빨간색 테두리로 표시되는가?
- [ ] 활성 청구서는 파란색 테두리로 표시되는가?

---

## 🧩 통합 시나리오 테스트

### 시나리오 1: 발주 → 청구서 → 결제 플로우
1. [ ] [발주병합] 시트에 발주 데이터 입력
2. [ ] 거래원장에서 해당 발주 확인
3. [ ] 청구서 생성
4. [ ] 결제관리에서 해당 청구서 번호로 입금 등록
5. [ ] 거래원장 상세에서 결제 내역 확인 가능

### 시나리오 2: 청구서 오류 → 취소/재발급
1. [ ] 청구서 생성
2. [ ] 청구서 취소/재발급 실행
3. [ ] 원본 청구서가 CANCELLED 상태로 변경
4. [ ] 새 청구서 생성 확인
5. [ ] 이력 조회로 전체 체인 확인

### 시나리오 3: 회사비용 관리
1. [ ] 여러 비용항목 추가 (임차료, 인건비, 통신비 등)
2. [ ] 항목별 통계 확인
3. [ ] 날짜 범위로 필터링
4. [ ] 특정 항목 삭제
5. [ ] 삭제 포함 조회로 확인

### 시나리오 4: 문서번호 자동완성
1. [ ] 청구서 여러 개 생성
2. [ ] 결제관리에서 문서번호 입력
3. [ ] 자동완성 제안 확인
4. [ ] 제안 선택하여 입력

---

## 🐛 에러 케이스 테스트

### 데이터 유효성
- [ ] 필수 필드 누락시 에러 메시지
- [ ] 금액에 음수 입력시 에러
- [ ] 날짜 형식 오류시 에러
- [ ] 존재하지 않는 ID로 수정/삭제시 에러

### 권한 및 보안
- [ ] 세션 사용자 정보가 올바르게 기록되는가?
- [ ] 삭제자, 입력자 필드가 정확한가?

### 성능
- [ ] 1000건 이상의 데이터로 조회 성능 확인
- [ ] 필터링 성능 확인
- [ ] 통계 계산 성능 확인

---

## ✅ 최종 확인 사항

### 코드 품질
- [ ] 모든 console.log가 Logger.log로 변경되었는가?
- [ ] 에러 핸들링이 모든 함수에 구현되어 있는가?
- [ ] 주석이 충분한가?

### 문서화
- [ ] README에 사용법이 기록되어 있는가?
- [ ] API 문서가 있는가?
- [ ] 아키텍처 문서가 업데이트되었는가?

### 배포
- [ ] 웹앱이 정상적으로 배포되는가?
- [ ] 모든 사용자가 접근 가능한가?
- [ ] 모바일에서도 정상 동작하는가?

---

## 📊 테스트 결과 기록

| 테스트 항목 | 상태 | 발견된 이슈 | 해결 방법 |
|------------|------|-------------|-----------|
| Phase 1: 데이터 구조 | ⬜ |  |  |
| Phase 2: 백엔드 서비스 | ⬜ |  |  |
| Phase 3: 프론트엔드 UI | ⬜ |  |  |
| Phase 4: 거래원장 통합 | ⬜ |  |  |
| Phase 5: 청구서 취소/재발급 | ⬜ |  |  |
| 통합 시나리오 | ⬜ |  |  |
| 에러 케이스 | ⬜ |  |  |

**범례:**
- ⬜ 미실행
- ✅ 통과
- ❌ 실패
- ⚠️ 부분 통과

---

## 📝 버그 리포트 템플릿

```
### 버그 제목
간단한 버그 설명

**재현 단계:**
1.
2.
3.

**예상 결과:**


**실제 결과:**


**스크린샷:**


**환경:**
- 브라우저:
- 날짜:
- 사용자:

**우선순위:** High / Medium / Low

**상태:** Open / In Progress / Resolved
```

---

## 🎯 테스트 완료 기준

모든 테스트가 완료되면:
1. [ ] 모든 Phase 테스트가 ✅ 통과
2. [ ] 발견된 버그가 모두 해결됨
3. [ ] 통합 시나리오가 모두 정상 동작
4. [ ] 문서화 완료
5. [ ] 배포 준비 완료

---

**테스트 시작일:** _____________
**테스트 완료일:** _____________
**테스터:** _____________
**최종 승인:** _____________
