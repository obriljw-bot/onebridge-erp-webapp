# 결제 관리 리뉴얼 - 최종 확정 명세서

**작성일:** 2025-12-26
**버전:** 1.0 FINAL
**상태:** ✅ 개발 승인 완료

---

## 🎯 확정된 아키텍처

### 데이터 연결 구조
```
결제내역 → 청구ID (필수) → 청구DB.orderNumbers (JSON 배열) → 거래원장 (복수)
```

**삭제:**
- ❌ 결제내역.발주번호 필드 (완전 제거)
- ❌ 발주번호 직접 입력 프로세스

**추가:**
- ✅ 임시 청구서 자동 생성 기능
- ✅ 청구서 검색 및 세부 확인 모달

---

## 📋 최종 의사결정 (14개 항목)

### A. 필수 결정사항

| # | 항목 | 확정 | 비고 |
|---|------|------|------|
| 1 | 청구서 없는 결제 | **B) 임시 청구서 자동 생성** | [임시 청구서 생성] 버튼 제공 |
| 2 | 모달 단계 | **A) 3단계** | 청구 검색 → 세부 확인 → 결제 입력 |
| 3 | 발주 상세 정보 | **B) 브랜드 단위까지** | 품목 세부는 제외 (과도한 정보) |
| 4 | 회사비용 UI | **B) inline form 유지** | 청구서 무관, 간단 입력 |
| 5 | 기존 데이터 마이그레이션 | **수동 정리** | 사용자 알림 + 청구서 발급 유도 |
| 6 | 청구ID 수정 | **A) 변경 불가** | 삭제 후 재입력 |

### B. 선택 결정사항

| # | 항목 | 확정 | 비고 |
|---|------|------|------|
| 7 | CANCELLED 청구서 결제 | **A) 차단** | 에러 메시지 표시 |
| 8 | PAID 청구서 추가 결제 | **B) 차단** | 이미 완료된 청구서 |
| 9 | DRAFT 청구서 결제 | **⏳ 보류** | 사용자 답변 대기 |
| 10 | 검색 성능 최적화 | **C) 추후** | 1,000건 미만, 추후 필요 시 |

### C. UI 상세 확정

| # | 항목 | 확정 |
|---|------|------|
| 11 | 검색 결과 추가 정보 | 브랜드명 포함 |
| 12 | 금액 불일치 시 | **A) 저장 차단** (엄격한 검증) |
| 13 | 모달 크기 | **B) 700px** |
| 14 | 검색 결과 정렬 | **A) 최신순** |

---

## 🏗️ 데이터 구조 상세

### 1. 발주 → 브랜드 → 품목 계층

```
발주번호: GMP-20251226-001 (최소 단위)
  ├─ 브랜드A
  │   ├─ 제품1 (수량, 금액)
  │   ├─ 제품2 (수량, 금액)
  │   └─ 제품3 (수량, 금액)
  └─ 브랜드B
      ├─ 제품4 (수량, 금액)
      └─ 제품5 (수량, 금액)

청구서: INV-20251226-001
  ├─ orderNumbers: ["GMP-001", "GMP-002", "GMP-003"]
  └─ 합계 금액: 4,500,000원
```

### 2. 청구서 세부 확인 화면 표시 범위

**✅ 표시 (브랜드 단위):**
```
발주번호        발주일       브랜드       금액
GMP-001      2025-12-01    브랜드A    2,000,000원
GMP-002      2025-12-10    브랜드B    1,500,000원
GMP-003      2025-12-20    브랜드C    1,000,000원
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
합계                                  4,500,000원
```

**❌ 제외 (품목 세부):**
```
발주번호: GMP-001
  └─ 브랜드A
      ├─ 제품1: 10개 × 50,000원
      ├─ 제품2: 20개 × 30,000원  ← 이런 세부는 안 보여줌
      └─ 제품3: 5개 × 100,000원
```

**이유:** 과도한 정보, 결제 입력 시 불필요

---

## 🎨 UI 설계 확정안

### Step 1: 청구서 검색 및 선택

```
┌─────────────────────────────────────────────────────────┐
│  💰 신규 입출금 기록 추가                         ✕      │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  [Step 1/3] 청구서 선택                                 │
│                                                         │
│  ┌───────────────────────────────────────────────────┐ │
│  │ 🔍 청구서 번호 또는 거래처명 검색                 │ │
│  │ [INV-20251226        ]  [검색]                    │ │
│  └───────────────────────────────────────────────────┘ │
│                                                         │
│  검색 결과:                                             │
│  ┌───────────────────────────────────────────────────┐ │
│  │ ○ INV-20251226-001                                │ │
│  │   거래처: 고객사A │ 브랜드: 브랜드A, 브랜드B       │ │
│  │   청구일: 2025-12-26 │ 금액: 4,500,000원          │ │
│  │   상태: ISSUED │ 발주 3건                          │ │
│  ├───────────────────────────────────────────────────┤ │
│  │ ○ INV-20251220-005                                │ │
│  │   거래처: 고객사A │ 브랜드: 브랜드C                │ │
│  │   청구일: 2025-12-20 │ 금액: 1,200,000원          │ │
│  │   상태: ISSUED │ 발주 1건                          │ │
│  └───────────────────────────────────────────────────┘ │
│                                                         │
│  [📄 청구서 없음 (임시 생성)]                           │
│                                                         │
│                                   [다음 단계 →]         │
└─────────────────────────────────────────────────────────┘
```

**검색 결과 표시 정보 (확정):**
- 청구ID
- 거래처명
- **브랜드명** (복수 시 쉼표 구분)
- 청구일
- 청구금액
- 상태 (DRAFT/ISSUED/PAID/CANCELLED)
- 발주 건수

---

### Step 2: 청구서 세부 확인 (브랜드 단위)

```
┌─────────────────────────────────────────────────────────┐
│  💰 신규 입출금 기록 추가                         ✕      │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  [Step 2/3] 청구서 상세 확인                            │
│                                                         │
│  선택된 청구서: INV-20251226-001                        │
│  거래처: 고객사A │ 청구일: 2025-12-26                   │
│                                                         │
│  ┌─ 포함된 발주 내역 ──────────────────────────────────┐ │
│  │ 발주번호      │ 발주일     │ 브랜드   │ 금액        │ │
│  ├──────────────┼───────────┼─────────┼─────────────┤ │
│  │ GMP-001      │ 2025-12-01 │ 브랜드A  │ 2,000,000원 │ │
│  │ GMP-002      │ 2025-12-10 │ 브랜드B  │ 1,500,000원 │ │
│  │ GMP-003      │ 2025-12-20 │ 브랜드C  │ 1,000,000원 │ │
│  ├──────────────┴───────────┴─────────┴─────────────┤ │
│  │ 합계                                  4,500,000원 │ │
│  └─────────────────────────────────────────────────┘ │
│                                                         │
│  💡 품목 세부 정보는 발주 상세보기에서 확인 가능합니다.   │
│                                                         │
│                         [← 이전]      [다음 단계 →]    │
└─────────────────────────────────────────────────────────┘
```

---

### Step 3: 결제 정보 입력 및 검증

```
┌─────────────────────────────────────────────────────────┐
│  💰 신규 입출금 기록 추가                         ✕      │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  [Step 3/3] 결제 정보 입력                              │
│                                                         │
│  청구서: INV-20251226-001 (고객사A)                     │
│  청구금액: 4,500,000원                                  │
│                                                         │
│  ┌─ 결제 정보 ──────────────────────────────────────┐ │
│  │ 결제일:     [2025-12-26      ] 📅                │ │
│  │ 결제유형:   (●) 입금  ( ) 출금                    │ │
│  │ 결제금액:   [4,500,000       ] 원                │ │
│  │ 결제수단:   [계좌이체 ▼]                          │ │
│  │ 비고:       [                ]                    │ │
│  └───────────────────────────────────────────────┘ │
│                                                         │
│  ┌─ 금액 검증 ──────────────────────────────────────┐ │
│  │ ✅ 결제금액과 청구금액이 일치합니다.              │ │
│  │                                                   │ │
│  │ 청구금액:   4,500,000원                           │ │
│  │ 결제금액:   4,500,000원                           │ │
│  │ 차액:              0원                            │ │
│  └───────────────────────────────────────────────┘ │
│                                                         │
│                         [← 이전]      [💾 저장]        │
└─────────────────────────────────────────────────────────┘
```

**금액 불일치 시 (저장 차단):**
```
┌─ 금액 검증 ──────────────────────────────────────┐
│ ❌ 결제금액과 청구금액이 일치하지 않습니다.      │
│                                                   │
│ 청구금액:   4,500,000원                           │
│ 결제금액:   5,000,000원                           │
│ 차액:       +500,000원 (초과)                     │
│                                                   │
│ 💡 금액을 수정해주세요.                           │
│    또는 청구서를 다시 확인해주세요.               │
└───────────────────────────────────────────────┘

[← 이전]  [💾 저장] ← 비활성화
```

---

## 🔧 임시 청구서 생성 기능

### 사용 시나리오

**Case 1: 선입금/보증금**
```
사용자: "청구서 발행 전인데 선입금 들어왔어"
→ [청구서 없음 (임시 생성)] 선택
→ 거래처, 금액 입력
→ INV-TEMP-20251226-001 자동 생성
→ 결제 입력
→ 나중에 정식 청구서 발행 후 연결
```

**Case 2: 긴급 결제**
```
사용자: "급하게 결제 입력해야 하는데 청구서 발행 시간 없어"
→ [임시 생성] 클릭
→ 간단 정보 입력
→ 임시 청구서 생성
→ 결제 입력 완료
```

### 임시 청구서 생성 모달

```
┌─────────────────────────────────────────────────────────┐
│  📄 임시 청구서 생성                              ✕      │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  💡 청구서가 없는 경우 임시 청구서를 생성합니다.        │
│     나중에 정식 청구서 발행 후 연결할 수 있습니다.      │
│                                                         │
│  ┌───────────────────────────────────────────────────┐ │
│  │ 거래처명:   [                ]  *                 │ │
│  │ 청구일:     [2025-12-26      ]  * 📅             │ │
│  │ 청구금액:   [                ]  * 원             │ │
│  │ 비고:       [임시 생성 (정식 청구서 발행 예정)]   │ │
│  └───────────────────────────────────────────────────┘ │
│                                                         │
│  ⚠️ 주의사항:                                           │
│  - 임시 청구서는 발주번호가 없습니다.                   │
│  - 정식 청구서 발행 후 반드시 연결해주세요.             │
│  - 임시 청구서는 DRAFT 상태로 생성됩니다.               │
│                                                         │
│                         [취소]      [📄 생성 후 계속]  │
└─────────────────────────────────────────────────────────┘
```

**생성 로직:**
```javascript
function createTempInvoice(params) {
  var invoiceId = 'INV-TEMP-' + formatDate(new Date()) + '-' + getNextSeq();

  var invoiceData = {
    invoiceId: invoiceId,
    billingType: 'DIRECT',
    orderNumbers: '[]',  // 빈 배열
    company: params.company,
    type: params.type,  // PURCHASE or SALES
    amount: params.amount,
    invoiceDate: params.date,
    status: 'DRAFT',  // 임시는 DRAFT
    notes: '임시 생성 (정식 청구서 발행 예정)',
    createdAt: new Date(),
    createdBy: Session.getActiveUser().getEmail()
  };

  // 청구DB에 저장
  saveInvoice(invoiceData);

  return {
    success: true,
    invoiceId: invoiceId,
    message: '임시 청구서가 생성되었습니다.'
  };
}
```

---

## 📊 청구서 상태별 결제 입력 제약

### 확정된 규칙

| 청구서 상태 | 결제 입력 | 동작 |
|------------|----------|------|
| **DRAFT** | ⏳ 보류 | 사용자 답변 대기 |
| **ISSUED** | ✅ 허용 | 정상 처리 |
| **PAID** | ❌ 차단 | "이미 결제 완료된 청구서입니다" |
| **CANCELLED** | ❌ 차단 | "취소된 청구서입니다" |

### 검증 로직

```javascript
function validateInvoiceForPayment(invoice) {
  switch(invoice.status) {
    case 'CANCELLED':
      return {
        valid: false,
        error: '❌ 취소된 청구서입니다.\n다른 청구서를 선택해주세요.'
      };

    case 'PAID':
      return {
        valid: false,
        error: '❌ 이미 결제 완료된 청구서입니다.\n추가 결제가 필요한 경우 새 청구서를 발행해주세요.'
      };

    case 'DRAFT':
      // ⏳ 사용자 답변 대기 (A: 차단 vs B: 경고만)
      // 임시 결정: 경고만 표시하고 허용
      return {
        valid: true,
        warning: '⚠️ 미발행 청구서입니다.\n청구서 발행 후 결제 입력을 권장합니다.'
      };

    case 'ISSUED':
      return { valid: true };

    default:
      return { valid: true };
  }
}
```

---

## 🔄 기존 데이터 처리 방식

### 확정: 수동 정리 (사용자 알림)

**방식:**
1. 청구ID 없는 기존 결제 데이터 조회
2. 사용자에게 알림 표시
3. 청구서 발급 유도

### 알림 UI

**입출금 관리 페이지 상단:**
```
┌─────────────────────────────────────────────────────────┐
│ ⚠️ 청구서 미연결 결제 내역 발견                         │
├─────────────────────────────────────────────────────────┤
│                                                         │
│ 총 23건의 결제 내역이 청구서와 연결되지 않았습니다.      │
│                                                         │
│ [미연결 내역 보기]  [일괄 청구서 생성]  [나중에]        │
└─────────────────────────────────────────────────────────┘
```

**미연결 내역 보기 모달:**
```
┌─────────────────────────────────────────────────────────┐
│  ⚠️ 청구서 미연결 결제 내역                      ✕      │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  아래 결제 내역들이 청구서와 연결되지 않았습니다.        │
│  청구서를 생성하거나 기존 청구서와 연결해주세요.         │
│                                                         │
│  ┌───────────────────────────────────────────────────┐ │
│  │ 결제일      │ 거래처   │ 금액        │ 작업       │ │
│  ├────────────┼─────────┼────────────┼────────────┤ │
│  │ 2025-12-20 │ 고객사A  │ 2,000,000원 │ [청구서 생성]│ │
│  │ 2025-12-15 │ 고객사B  │ 1,500,000원 │ [청구서 생성]│ │
│  │ 2025-12-10 │ 공급사C  │ 1,000,000원 │ [청구서 생성]│ │
│  └───────────────────────────────────────────────────┘ │
│                                                         │
│  [일괄 청구서 생성 (23건)]           [닫기]             │
└─────────────────────────────────────────────────────────┘
```

**일괄 청구서 생성 확인:**
```
┌─────────────────────────────────────────────────────────┐
│  📄 일괄 임시 청구서 생성 확인                    ✕      │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  23건의 결제 내역에 대해 임시 청구서를 생성합니다.       │
│                                                         │
│  ⚠️ 주의사항:                                           │
│  - 각 결제마다 임시 청구서(INV-TEMP-XXX)가 생성됩니다.  │
│  - 임시 청구서는 DRAFT 상태로 생성됩니다.               │
│  - 추후 정식 청구서 발행 후 연결을 권장합니다.          │
│                                                         │
│  계속 진행하시겠습니까?                                 │
│                                                         │
│                         [취소]      [📄 생성 (23건)]   │
└─────────────────────────────────────────────────────────┘
```

### 일괄 생성 로직

```javascript
function batchCreateTempInvoices() {
  // 청구ID 없는 결제 조회
  var paymentsWithoutInvoice = getPaymentRecords({
    filter: function(p) {
      return !p.docNumber || p.docNumber === '';
    }
  });

  var created = 0;
  var errors = [];

  paymentsWithoutInvoice.forEach(function(payment) {
    try {
      // 임시 청구서 생성
      var invoiceResult = createTempInvoice({
        company: payment.company,
        type: payment.type === '입금' ? 'SALES' : 'PURCHASE',
        amount: payment.amount,
        date: payment.date
      });

      if (invoiceResult.success) {
        // 결제 레코드 업데이트
        updatePaymentRecord({
          paymentId: payment.paymentId,
          docNumber: invoiceResult.invoiceId
        });
        created++;
      }
    } catch (error) {
      errors.push({
        paymentId: payment.paymentId,
        error: error.message
      });
    }
  });

  return {
    success: true,
    created: created,
    errors: errors,
    message: created + '건의 임시 청구서가 생성되었습니다.'
  };
}
```

---

## 🚀 개발 단계별 계획

### Phase 1: 백엔드 기반 구축 (2일)

**구현 함수:**
```javascript
// 1. 청구서 검색
function searchInvoices(params) {
  // params: { query, company (optional) }
  // return: { success, invoices: [{ invoiceId, company, brands, date, amount, status, orderCount }] }
}

// 2. 청구서 상세 조회
function getInvoiceDetail(invoiceId) {
  // return: { success, invoice: { ...기본 정보, orderNumbers, orders: [{ orderNumber, brand, amount, date }] } }
}

// 3. 임시 청구서 생성
function createTempInvoice(params) {
  // params: { company, type, amount, date }
  // return: { success, invoiceId }
}

// 4. 청구서 상태 검증
function validateInvoiceForPayment(invoice) {
  // return: { valid, error, warning }
}

// 5. 브랜드 정보 조회 (발주별)
function getOrderBrands(orderNumber) {
  // 거래원장에서 발주번호의 브랜드 목록 조회
  // return: [{ brand, amount }]
}
```

**ApiService.js 래퍼:**
```javascript
function searchInvoicesApi(params) { return safeReturn(searchInvoices(params)); }
function getInvoiceDetailApi(invoiceId) { return safeReturn(getInvoiceDetail(invoiceId)); }
function createTempInvoiceApi(params) { return safeReturn(createTempInvoice(params)); }
```

---

### Phase 2: 모달 UI 구현 (3일)

**파일:** CommonScripts.html

**구현 함수:**
```javascript
// 1. 모달 오픈
function openPaymentModal()

// 2. Step 1: 청구서 검색
function renderInvoiceSearch()
function searchInvoicesUI(query)
function selectInvoice(invoiceId)

// 3. Step 2: 청구서 세부 확인
function renderInvoiceDetail(invoice)
function showOrderBrands(orders)

// 4. Step 3: 결제 정보 입력
function renderPaymentForm(invoice)
function validatePaymentAmount()
function savePaymentFromModal()

// 5. 임시 청구서 생성
function openTempInvoiceModal()
function createTempInvoiceUI(params)

// 6. 모달 네비게이션
function goToStep(step)
function closePaymentModal()
```

**CSS 스타일:**
```css
.payment-modal-overlay { }
.payment-modal-content { width: 700px; }
.payment-modal-step { }
.invoice-search-results { }
.invoice-detail-table { }
.payment-form-group { }
.amount-validation { }
.amount-match { color: #059669; }
.amount-mismatch { color: #dc2626; }
```

---

### Phase 3: 기존 폼 제거 및 연동 (1일)

**수정 파일:** Page_PaymentManagement.html

**Before:**
```html
<!-- 기존 inline form -->
<div class="payment-form-container">
  <input id="payment-date" />
  <select id="payment-type"></select>
  <!-- ... -->
  <button onclick="handleAddPayment()">추가</button>
</div>
```

**After:**
```html
<!-- 모달 오픈 버튼만 -->
<div class="payment-header">
  <h2>💰 입출금 관리</h2>
  <button class="payment-btn primary" onclick="openPaymentModal()">
    💰 신규 결제 입력
  </button>
</div>
```

**결제내역 시트 구조 변경:**
```javascript
// SetupPaymentSheets.js
var headers = [
  '결제ID',
  '결제일',
  '결제유형',
  '거래처명',
  '금액',
  '결제수단',
  '문서번호',        // 청구ID (필수)
  // '발주번호',     // ❌ 삭제
  '비고',
  '삭제여부',
  '삭제일시',
  '삭제자',
  '입력일시',
  '입력자'
];
```

---

### Phase 4: 기존 데이터 처리 UI (1일)

**구현:**
1. 미연결 결제 조회 함수
2. 알림 배너
3. 미연결 내역 모달
4. 일괄 청구서 생성 함수
5. 개별 청구서 생성 버튼

**함수:**
```javascript
function getUnlinkedPayments()
function showUnlinkedPaymentsAlert()
function openUnlinkedPaymentsModal()
function batchCreateTempInvoices()
function createTempInvoiceForPayment(paymentId)
```

---

### Phase 5: 테스트 및 검증 (1일)

**테스트 시나리오:**

1. **정상 결제 입력:**
   - 청구서 검색
   - 세부 확인
   - 결제 입력
   - 금액 일치 확인
   - 저장 성공

2. **금액 불일치:**
   - 결제 금액 다르게 입력
   - 저장 버튼 비활성화 확인
   - 에러 메시지 표시 확인

3. **임시 청구서 생성:**
   - 임시 생성 버튼
   - 정보 입력
   - INV-TEMP-XXX 생성 확인
   - 결제 입력 완료

4. **청구서 상태 검증:**
   - CANCELLED 청구서 선택 → 차단 확인
   - PAID 청구서 선택 → 차단 확인
   - DRAFT 청구서 선택 → 경고 확인 (허용)

5. **기존 데이터 처리:**
   - 미연결 알림 표시 확인
   - 일괄 생성 버튼 동작
   - 개별 생성 버튼 동작

6. **브랜드 정보 표시:**
   - 검색 결과에 브랜드 표시
   - 세부 확인에서 발주별 브랜드 표시
   - 브랜드 단위 금액 집계

---

## 📏 코드 품질 기준

### 검증 규칙

1. **필수 입력 검증:**
   ```javascript
   if (!invoiceId) {
     return { success: false, error: '청구서를 선택해주세요.' };
   }
   if (!amount || amount <= 0) {
     return { success: false, error: '올바른 금액을 입력해주세요.' };
   }
   ```

2. **금액 일치 검증 (엄격):**
   ```javascript
   if (paymentAmount !== invoiceAmount) {
     return {
       valid: false,
       error: '결제금액과 청구금액이 일치하지 않습니다.\n금액을 수정해주세요.'
     };
   }
   ```

3. **청구서 상태 검증:**
   ```javascript
   if (invoice.status === 'CANCELLED' || invoice.status === 'PAID') {
     return { valid: false, error: '... 메시지 ...' };
   }
   ```

4. **사용자 피드백:**
   ```javascript
   // 성공
   alert('✅ 결제가 정상적으로 입력되었습니다.');

   // 실패
   alert('❌ ' + error.message);

   // 경고
   if (confirm('⚠️ ' + warning.message + '\n계속 진행하시겠습니까?')) {
     // 진행
   }
   ```

---

## 🎯 완료 조건

### Phase별 완료 체크리스트

**Phase 1 (백엔드):**
- [ ] `searchInvoices()` 구현 및 테스트
- [ ] `getInvoiceDetail()` 구현 및 테스트
- [ ] `createTempInvoice()` 구현 및 테스트
- [ ] `validateInvoiceForPayment()` 구현 및 테스트
- [ ] `getOrderBrands()` 구현 및 테스트
- [ ] API 래퍼 함수 추가

**Phase 2 (모달 UI):**
- [ ] 모달 HTML 구조 완성
- [ ] Step 1: 청구서 검색 UI
- [ ] Step 2: 청구서 세부 확인 UI
- [ ] Step 3: 결제 입력 UI
- [ ] 임시 청구서 생성 모달
- [ ] 모달 네비게이션 (이전/다음/닫기)
- [ ] CSS 스타일 완성

**Phase 3 (통합):**
- [ ] 기존 inline form 제거
- [ ] 신규 입력 버튼 추가
- [ ] 결제내역 시트 구조 변경
- [ ] 발주번호 컬럼 제거
- [ ] 데이터 저장 로직 업데이트

**Phase 4 (기존 데이터):**
- [ ] 미연결 결제 조회 함수
- [ ] 알림 배너 UI
- [ ] 미연결 내역 모달
- [ ] 일괄 생성 함수
- [ ] 개별 생성 버튼

**Phase 5 (테스트):**
- [ ] 6개 테스트 시나리오 통과
- [ ] 에러 케이스 검증
- [ ] 사용자 피드백 확인
- [ ] 브랜드 정보 표시 확인

---

## 📝 추가 확인 필요 (1개)

### ⏳ DRAFT 청구서 결제 허용 여부

**현재 상태:** 임시 결정 (경고만 표시하고 허용)

**질문:**
- DRAFT 청구서에 결제 입력을 허용할까요?
  - A) 차단 (청구서 발행 후 결제 입력 강제)
  - B) 경고만 표시하고 허용 (현재 임시 설정)

**실무 시나리오:**
- 내부 검토 중인 DRAFT 청구서에 선입금 들어온 경우
- 승인 대기 중인데 결제부터 들어온 경우

**결정 대기 중**

---

## 📊 예상 효과

### Before (현재)
```
사용자: "A업체 500만원 입금됐어"
→ 발주번호? (모름)
→ 공란으로 저장
→ 발주 상태 미동기화
→ 나중에 수정 불가
```

### After (리뉴얼)
```
사용자: "A업체 500만원 입금됐어"
→ [신규 결제 입력] 클릭
→ "A업체" 검색
→ 청구서 2건 표시
   INV-001 (브랜드A, B) 4,500,000원
   INV-002 (브랜드C) 1,200,000원
→ INV-001 선택
→ 발주 3건 확인 (브랜드별)
→ 금액 입력: 5,000,000원
→ ❌ 청구금액(4,500,000원)과 불일치
→ 금액 수정: 4,500,000원
→ ✅ 일치 확인
→ 저장 완료
→ 3건 발주 상태 자동 업데이트
```

---

## 🚀 개발 준비 완료

**총 개발 기간:** 8일
- Phase 1: 2일
- Phase 2: 3일
- Phase 3: 1일
- Phase 4: 1일
- Phase 5: 1일

**승인 대기 사항:**
- ⏳ DRAFT 청구서 결제 허용 여부 (A or B)

**답변 받으면 즉시 개발 시작합니다!**
